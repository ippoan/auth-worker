{
  "name": "@ippoan/auth-client-worker",
  "version": "0.1.0",
  "description": "OAuth client SDK for Cloudflare Worker consumers of auth-worker (PKCE + DCR + introspect)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "peerDependencies": {
    "@cloudflare/workers-types": ">=4"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ippoan/auth-worker.git",
    "directory": "packages/auth-client-worker"
  }
}
